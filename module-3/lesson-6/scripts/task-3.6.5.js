let fName = document.querySelector('input[name="full-name"]');


let button = document.querySelector('button');
    button.addEventListener('click', function(event) {
        alert(fName.value)
    }) ;
